<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="Purchase complete — The Infinite Crossroads: A TTRPG." />
  <title>Acquisition Complete — The Infinite Crossroads</title>
  <link rel="icon" href="../../assets/Icons/crossroads_compass.png" />
  <style>
    :root {
      --ink: #070b10;
      --navy: #111c28;
      --brass: #c6a15b;
      --brass-bright: #e0c47d;
      --ivory: #eee7d5;
      --muted: #b6aa8c;
    }
    * { box-sizing: border-box; }
    body {
      margin: 0;
      min-height: 100vh;
      display: grid;
      place-items: center;
      padding: 24px;
      color: var(--ivory);
      background:
        linear-gradient(rgba(3, 6, 10, .92), rgba(3, 6, 10, .98)),
        url("../../assets/InfiniteCrossroadsHeader.png") center / cover fixed no-repeat;
      font-family: Georgia, "Times New Roman", serif;
      line-height: 1.55;
    }
    .panel {
      width: min(760px, 100%);
      padding: clamp(28px, 6vw, 58px);
      text-align: center;
      background: linear-gradient(145deg, rgba(20,31,42,.97), rgba(8,13,18,.99));
      border: 1px solid rgba(198,161,91,.72);
      box-shadow: 0 24px 60px rgba(0,0,0,.72), inset 0 0 30px rgba(0,0,0,.45);
    }
    .kicker {
      color: var(--brass-bright);
      text-transform: uppercase;
      letter-spacing: .2em;
      font-size: .78rem;
    }
    h1 {
      margin: 12px 0 18px;
      font-weight: 500;
      font-size: clamp(2.2rem, 7vw, 4.2rem);
      line-height: 1;
    }
    p { color: #d8cfba; }
    .important {
      margin: 28px auto;
      padding: 18px;
      max-width: 590px;
      border: 1px solid rgba(198,161,91,.4);
      background: rgba(5,9,13,.6);
    }
    .btn {
      display: inline-flex;
      min-height: 48px;
      align-items: center;
      justify-content: center;
      padding: 12px 22px;
      border: 1px solid var(--brass);
      background: linear-gradient(#3e2f1b, #1c150d);
      color: var(--ivory);
      text-decoration: none;
      text-transform: uppercase;
      letter-spacing: .1em;
      font-weight: 700;
    }
    .small {
      margin-top: 28px;
      color: var(--muted);
      font-size: .9rem;
    }
  </style>
</head>
<body>
  <main class="panel">
    <div class="kicker">Schola Archive // Acquisition Recorded</div>
    <h1>Acquisition Complete</h1>
    <p>
      Your payment for <em>The Infinite Crossroads: A TTRPG</em> has been received.
    </p>
    <div class="important">
      <strong>Your private download link is sent by email.</strong><br />
      Please allow a few minutes for delivery and check your spam or promotions folder if it does not appear.
    </div>
    <p>
      The download link is private and expires after 48 hours.
    </p>
    <p>
      <a class="btn" href="../ledger.html">Return to the System Ledger</a>
    </p>
    <p class="small">
      Thank you for supporting Otherworldly Publishing. Safe travels through the Crossroads.
    </p>
  </main>
</body>
</html>
